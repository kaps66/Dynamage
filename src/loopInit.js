/*
  loopInit.js (tombstone)
  The original large runtime implementation was refactored into focused modules (loopMain.js, recording.js, intro.js, logoEgg.js, resize.js, background.js, etc.).
  Remaining content removed to keep this file as a lightweight compatibility shim.
  Removed large initialization & loop implementations from this file.
*/

 // delegate to the refactored entry point
import "./loopMain.js";

